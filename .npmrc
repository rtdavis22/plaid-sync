# Pin to the public registry. Without this, a machine configured against a
# private proxy writes proxy URLs into package-lock.json, and `npm ci` in CI
# then fails with a 401 it cannot recover from.
registry=https://registry.npmjs.org/
